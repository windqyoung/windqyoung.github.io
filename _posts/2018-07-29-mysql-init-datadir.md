---
title: 初始化一个全新的数据库目录
---
```
执行命令:
`mysqld --initialize --datadir=/var/mysql/data1`
密码在`/var/mysql/data1/*.err`里面查看

执行命令:
`mysqld --initialize-insecure --datadir=/var/mysql/data2`
root用户无密码


```
